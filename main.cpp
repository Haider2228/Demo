// main.cpp
#include <iostream>

int main() {
    int a, b;

    // Get input from the user
    std::cout << "Enter two numbers: ";
    std::cin >> a >> b;

    // Calculate the sum
    int sum = a + b;

    // Output the result
    std::cout << "The sum is: " << sum << std::endl;

    return 0;
}
